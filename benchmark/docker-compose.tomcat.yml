version: '3'
services:
  acmeair-db:
    container_name: acmeair-db
    image: mongo
    ports:
      - "27020:27020"
    networks:
      - acme

  tomcat:
    container_name: acmeair-tomcat
    image: acmeair-tomcat
    networks:
      - acme
    ports:
      - "9080:8080"
      - "9443:8443"
      - "7777:7777"
    environment:
      - MONGO_HOST=acmeair-db

networks:
  acme:
    driver: bridge
